version: "3"
services:
  web:
    image: mozilla/send:latest
    links:
      - redis
    ports:
      - "127.0.0.1:1443:1443"
    environment:
      - REDIS_HOST=redis
      - NODE_ENV=production
      - FILE_DIR=/data/send
  redis:
    image: redis:alpine
